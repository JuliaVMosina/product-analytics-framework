name: 'health_analytics'
version: '1.0.0'
config-version: 2

profile: 'health_analytics'

model-paths: ['models']
test-paths: ['tests']
docs-paths: ['docs']

models:
  health_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
